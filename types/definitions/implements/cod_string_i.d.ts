import { Cod_string } from "../interfaces/cod_string";
export declare class Cod_string_i implements Cod_string {
    randomStr(min: number, max: number): string;
    substr(str: string, len: number): string;
}
